ion-view(view-title="")
	ion-content
		p How many hours till the event starts? (or do you want to get funky right now?)
		.row
		.row.row-center
			.col
				.list.list-inset
					label.item.item-input.padding
						input(type="number", ng-model="startTimeHours", min=0, placeholder="Hours")
					label.item.item-input.padding
						input(type="number", ng-model="startTimeMinutes", min=0, max=59, placeholder="Minutes")
					button.button.button-positive.button-full(ng-click="setTimer(startTimeHours, startTimeMinutes)", ng-init="channelName=random_name") Create {{channelName}}