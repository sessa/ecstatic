ion-view(view-title="")
	ion-content
		p
		.row
		.row.row-center
			.col
				.list.list-inset
					p What do you want to call you channel?
					label.item.item-input.padding
						input(type="text", placeholder="Type a name here.", ng-model="channelName")					button.button.button-positive.button-full(ng-click="create_channel(channelName)", ng-init="channelName=random_name") Create {{channelName}}
		.row.row-center
			.col
				.list.list-inset(ng-controller="CountdownCtrl")
					label.item.item-input.padding
						input(type="number", ng-model="startTimeHours", min=0, placeholder="How many hours till the event starts?")
					label.item.item-input.padding
						input(type="number", ng-model="startTimeMinutes", min=0, max=59, placeholder="How many minutes till the event starts?")
					button.button.button-positive.button-full(ng-click="create_timer(startTimeHours, startTimeMinutes, channelName)", ng-init="channelName=random_name") Create {{channelName}}